default:
  image:
    name: hashicorp/terraform
    entrypoint: [""] # to override the default entrypoint command
  timeout: 15 minutes

stages:
  - format

format-job:
  stage: format
  script:
    - terraform fmt
